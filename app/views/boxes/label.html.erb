<!DOCTYPE html>
<html>
  <head>
    <title>Étiquette Carton #<%= @box.number %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 20px;
        background: white;
      }
      .label {
        border: 3px solid #000;
        padding: 20px;
        max-width: 400px;
        margin: 0 auto;
      }
      .box-number {
        font-size: 48px;
        font-weight: bold;
        text-align: center;
        margin-bottom: 20px;
        border: 2px solid #000;
        padding: 10px;
      }
      .destination {
        font-size: 24px;
        font-weight: bold;
        text-align: center;
        margin-bottom: 15px;
        background: #f0f0f0;
        padding: 10px;
      }
      .origin {
        font-size: 18px;
        text-align: center;
        margin-bottom: 15px;
        color: #666;
      }
      .description {
        font-size: 14px;
        margin-bottom: 15px;
        border-top: 1px solid #ccc;
        padding-top: 10px;
      }
      .stats {
        display: flex;
        justify-content: space-between;
        font-size: 12px;
        color: #666;
        border-top: 1px solid #ccc;
        padding-top: 10px;
      }
      .qr-placeholder {
        width: 80px;
        height: 80px;
        border: 2px dashed #ccc;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 10px;
        color: #666;
        text-align: center;
        margin: 10px auto;
      }
      @media print {
        body { margin: 0; }
        .label { max-width: none; }
      }
    </style>
  </head>
  <body>
    <div class="label">
      <div class="box-number">
        CARTON #<%= @box.number %>
      </div>

      <div class="destination">
        → <%= @box.destination_room.upcase %>
      </div>

      <div class="origin">
        Depuis: <%= @box.room.name %>
      </div>

      <div class="description">
        <strong>Contenu:</strong><br>
        <%= @box.description %>
      </div>

      <div class="qr-placeholder">
        QR CODE
      </div>

      <div class="stats">
        <span><%= @box.items_count %> objets</span>
        <span><%= number_to_currency(@box.total_value, unit: "€") %></span>
        <span><%= Date.current.strftime("%d/%m/%Y") %></span>
      </div>
    </div>

    <script>
      // Auto-print when page loads
      window.onload = function() {
        window.print();
      };
    </script>
  </body>
</html>
